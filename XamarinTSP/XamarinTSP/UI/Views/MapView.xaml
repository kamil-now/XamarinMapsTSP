<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:customControls="clr-namespace:XamarinTSP.UI.CustomControls"
             x:Class="XamarinTSP.UI.Views.MapView">
  <ContentView.Content>
        <customControls:CustomMap BindingContext="{Binding }"
                                  RouteCoordinates="{Binding CalculatedRoute.RouteCoordinates}"
                                  Position="{Binding MapPosition}"
                                  Locations="{Binding List.Locations}"
                                  IsRouteVisible="{Binding IsRouteVisible}"/>
    </ContentView.Content>
</ContentView>